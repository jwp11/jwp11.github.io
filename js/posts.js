/* ============================================
   文章数据文件 —— 在这里新增/修改你的文章
   格式说明:
     id:      文章唯一标识(英文数字,用于网址 ?id=xxx)
     title:   标题
     date:    发布日期
     tags:    标签数组
     summary: 摘要(首页卡片显示)
     content: 正文,支持轻量 Markdown 语法:
              # ## ### 标题 | **粗体** | *斜体* | `行内代码`
              ```语言 代码块``` | > 引用 | - 列表 | [链接](地址)
   ============================================ */

const POSTS = [
  {
    id: "rk3566-camera-isp-h264-pipeline",
    title: "从 Sensor 到屏幕:泰山派 RK3566 摄像头 → ISP → 硬件 H.264 → 推流 → 解码显示的完整链路",
    date: "2026-09-17",
    tags: ["嵌入式Linux", "RK3566", "GStreamer", "摄像头", "ISP", "硬件编码"],
    summary: "把一颗 MIPI 摄像头变成电脑/手机上能看的实时画面,中间要经过采集、ISP、硬件编码、网络封装、解码显示五环,外加一条 3A 控制回路。这篇把整条链路和涉及的名词(MIPI CSI、Bayer RAW、ISP、NV12、H.264、RTP/RTSP/RTMP/WebRTC、HDMI 分支)逐个讲清楚,并给出每环的设备节点、可执行命令、验证方法与常见坑。",
    content: `
把一块 RK3566(立创·泰山派)加一颗 OV8858 MIPI 摄像头做成能实时推流的"运动相机",整条链路可以完整画成下面这样——注意它是**两条链路**:一条搬图像数据,一条做 3A 控制。

> 本篇讲**原理与完整链路**。如果你要看这块板子从串口、烧写、DTB、接 OV8858 到 3A 踩坑的实操记录,另有 [泰山派 RK3566 运动相机实战手册](tspi-camera.html)。

## 一、总览:完整链路图

\`\`\`text
光线
 ↓
OV8858 图像传感器
 ↓  MIPI CSI-2,4 Lane
RK3566 CSI 接收器
 ↓
RK3566 ISP 图像处理器
 ↓
NV12 原始视频帧
 ↓
RK3566 硬件 H.264 编码器
 ↓
RTP / RTSP / RTMP / WebRTC
 ↓
网络
 ↓
电脑或手机解码显示
\`\`\`

另外还有一条 **3A 控制链路**(它不搬像素,只调参数):

\`\`\`text
ISP 统计画面亮度
 ↓
rkaiq_3A_server
 ↓
通过 I2C 调整 OV8858 的曝光、增益和白平衡
\`\`\`

两条链路合到一张图里:

\`\`\`text
        ┌─────────────────── 3A 控制链路 ───────────────────┐
        │                                                   │
        ▼            MIPI CSI-2 (4 Lane)                    │
┌──────────────┐  ───────────────────►  ┌──────────┐  NV12  ┌──────────┐  H.264  ┌───────────┐
│   OV8858     │                        │  RK3566  │ ─────► │ RK3566   │ ─────► │ 网络封装   │ ──► 网络 ──► 电脑/手机
│ 图像传感器   │  ◄─── I2C(曝光/增益) ── │   ISP    │        │ 硬件编码  │        │ RTP/RTSP  │            解码显示
└──────────────┘                        └──────────┘        └──────────┘        └───────────┘
        ▲                                    │
        │                              rkaiq_3A_server
        └────────────────────────────────────┘
\`\`\`

把五环的"输入/输出/负责硬件"列出来:

| 环节 | 输入 | 输出 | 负责硬件/软件 |
|---|---|---|---|
| ① 采集 | 光子 | Bayer RAW(如 RAW10) | OV8858 + MIPI CSI-2 |
| ② ISP | Bayer RAW | **NV12**(YUV 4:2:0) | RK3566 ISP |
| ③ 编码 | NV12 | **H.264 码流** | RK3566 硬件编码器(VENC/MPP) |
| ④ 封装 | H.264 NAL | **RTP 包**(UDP) | GStreamer rtph264pay + udpsink |
| ⑤ 解码显示 | RTP 包 | 屏幕像素 | 电脑/手机的 VLC 等播放器 |

下面按这个顺序逐个讲。

## 二、① OV8858:把光转换成原始图像

OV8858 是摄像头**传感器**,负责把光线转换成电信号。

它输出的**不是** NV12,而是 **Bayer RAW** 数据(例如 RAW10)——可以理解为"传感器只提供最原始的红绿蓝感光数据",每个像素只有一个颜色分量,还带大量噪点,不能直接显示。

OV8858 的**曝光时间、模拟增益**等参数,是通过 **I2C** 配置的:

\`\`\`text
rkaiq_3A_server ──I2C──► OV8858
\`\`\`

举例:如果自动曝光(AE)正常工作,白天对着天空拍时,传感器会**自动降低曝光和增益**,避免过曝。

这一环要配对的东西:

- **驱动**:内核里要有 \`ov8858\` 驱动(defconfig 里的 \`CONFIG_VIDEO_OV8858\`)
- **上电时序**:复位、PWDN、供电三个 GPIO 按手册顺序拉
- **设备树**:i2c 地址(本模组 \`4-0036\`)、MIPI lane 数、时钟
- **数据出口**:MIPI CSI-2

验证:

\`\`\`bash
dmesg | grep -iE "ov8858|csi2-dphy"
# 期望: ov8858 4-0036: Detected OV008858 sensor, REVISION 0xb2
\`\`\`

> 一个常见约束:这颗 sensor 在 **4-lane 模式下只支持 3264x2448@30 一种分辨率**,想要 1080p 得靠 ISP 缩放,而不是让 sensor 直接输出 1080p。

## 三、② MIPI CSI:把摄像头数据高速传给 RK3566

**MIPI CSI-2** 是摄像头和 SoC 之间的**高速串行接口**。硬件大致是:

\`\`\`text
OV8858 ──► 4 条 MIPI 数据 Lane ──► RK3566 CSI 接收器
\`\`\`

关键在于**它只负责"传输"**,不负责:

- 自动曝光
- 图像美化
- H.264 压缩
- 网络发送

它只是把 OV8858 输出的原始图像数据高速送到 RK3566。所以画面黑不黑、偏不偏色、压缩得怎么样,都跟 CSI 无关。

> 排查提示:如果 \`dmesg\` 报 \`No link between dphy and sensor\`,是设备树里的 lane/endpoint 配对错了,与 CSI 传输本身无关。

## 四、③ ISP:把原始数据处理成可用图像

**ISP**(Image Signal Processor,图像信号处理器)是整条链路里最"重"的一环。它会做很多工作:

- Bayer RAW 转 RGB/YUV
- 去马赛克(Demosaic)
- 自动曝光配合(配合 3A)
- 自动白平衡
- 降噪
- 锐化
- 镜头校正
- 色彩处理
- 缩放
- 输出 NV12

可以理解为:

\`\`\`text
OV8858 原始 RAW ──► ISP ──► 正常的彩色视频帧
\`\`\`

在你的系统里,**\`/dev/video0\` 就是 ISP 的主输出节点**(mainpath)。

### 4.1 拓扑:用 media controller 看设备怎么连

Linux 把这条链路建模成一张"媒体图",用 \`media-ctl\` 就能看到所有节点和连接:

\`\`\`bash
media-ctl -d /dev/media0 -p
\`\`\`

典型结构:

\`\`\`text
ov8858(4-0036)          ← 子设备:/dev/v4l-subdev3(曝光/增益)
   └─► rockchip-csi2-dphy0
        └─► rkisp-csi-subdev
             └─► rkisp-isp-subdev   ← ISP 本体(3A、缩放在这生效)
                  ├─► rkisp_mainpath → /dev/video0   主输出(我们要的)
                  └─► rkisp_selfpath → /dev/video1   副输出
dw9714(4-000c)          ← 对焦马达 VCM:/dev/v4l-subdev4(focus_absolute)
\`\`\`

设一个目标格式试试:

\`\`\`bash
v4l2-ctl -d /dev/video0 --set-fmt-video=width=1920,height=1080,pixelformat=NV12
\`\`\`

> 关键:**ISP 输出分辨率与 sensor 采集分辨率解耦**。可以让 ISP 输出 1920x1080,而 sensor 仍按 8MP 采集。

### 4.2 3A 服务:不常驻,画面就是黑的

这是新手最容易翻车的地方。ISP 硬件本身**不会自动算曝光和白平衡**,需要一个用户态程序 **\`rkaiq_3A_server\`** 常驻后台,它:

- 通过 \`/dev/video7\` 读 ISP 统计(亮度直方图、白平衡统计)
- 通过 \`/dev/video8\` 把算好的参数写回 ISP/sensor

所以顺序必须是:**先起 3A,再开应用**。

\`\`\`bash
/usr/bin/rkaiq_3A_server < /dev/null > /tmp/3a.log 2>&1 &
\`\`\`

两个硬性约束:

1. **只能有一套 3A**。同时跑两个(比如外部一个 + 某框架内部自带的)会抢着写 \`/dev/video8\`,画面一直闪绿。
2. **别自己写软件 AE 去抢曝光**。rkaiq 会每帧覆盖 sensor 曝光寄存器,你用 \`v4l2-ctl -c exposure=...\` 设完,几秒后读回来就变了。曝光/白平衡统一交给 rkaiq。

> 日志里看到 \`aec stats is null\` / \`awb stats is null\` 之类报错,**只要只在启动头几帧出现就正常**,3A 稳定后统计就通了。

验证:

\`\`\`bash
media-ctl -d /dev/media0 -p | grep -i enabled
# 1080p NV12 一帧 = 1920*1080*1.5 = 3110400 字节
v4l2-ctl -d /dev/video0 --set-fmt-video=width=1920,height=1080,pixelformat=NV12 \\
        --stream-mmap --stream-count=5 --stream-to=/tmp/frame.nv12
ls -l /tmp/frame.nv12     # 期望 15552000 = 3110400 * 5
\`\`\`

## 五、④ NV12:ISP 输出的原始视频格式

**NV12 是图像帧的排列方式**,不是接口,也不是压缩格式。它由两部分组成:

- **Y 平面**:亮度
- **UV 平面**:颜色(色度以 2x2 抽样,所以只有 Y 的 1/2 大小)

\`\`\`text
YYYYYYYYYYYY
YYYYYYYYYYYY
UVUVUVUVUVUV
\`\`\`

1920×1080 的 NV12 单帧大小:

\`\`\`text
1920 × 1080 × 1.5 ≈ 3.1 MB
30fps 时:3.1 MB × 30 ≈ 93 MB/s
\`\`\`

所以 **NV12 不能直接通过普通 Wi-Fi 上传**——它只是 ISP 和编码器之间的**中间数据**。

## 六、⑤ 硬件 H.264:把 NV12 压缩成小数据

RK3566 内部有**硬件视频编码能力**,通常通过 Rockchip **MPP**(Media Process Platform)或 GStreamer 的元件 **\`mpph264enc\`** 使用。

数据变化:

\`\`\`text
NV12 原始帧 ──► 硬件 H.264 编码器 ──► H.264 压缩码流
\`\`\`

量级对比:

| 形态 | 数据量 |
|---|---|
| 原始 NV12(1080p30) | ≈ 93 MB/s |
| H.264 压缩后 | 约 2~6 Mbps(≈ 0.25~0.75 MB/s) |

H.264 靠两种压缩:

- **空间压缩**:相邻像素相似
- **时间压缩**:相邻视频帧变化不大
- **I 帧**:完整图像(关键帧)
- **P 帧**:只记录相对前一帧的变化

常见参数:

| 参数 | 含义 | 典型值 |
|---|---|---|
| 码率 | 目标带宽 | 2~6 Mbps |
| 帧率 | 每秒帧数 | 30 fps |
| GOP | 关键帧间隔 | 30 |
| \`header-mode\` | SPS/PPS 头怎么发 | \`each-idr\` |

> **H.265 也可以进一步降低码率,但电脑和手机的兼容性不如 H.264**,所以建议先把 H.264 做通。

### 6.1 零拷贝的坑:为什么"直接接"会全绿

硬件编码器为了省带宽,默认会对上游缓冲做**零拷贝导入**:它发现输入是 DMABuf,就直接拿文件描述符让 VPU 去映射。

问题出在 **IOMMU 分组**:RK3566 上 \`rkisp\` 与 \`rkvenc\` 分属**不同 IOMMU 组**,VPU 在自己的域里映射不到 ISP 的页,读到的是**未映射的全零内存** → 编出 \`YUV(0,0,0)\` → 解码后整屏纯绿:

![零拷贝跨 IOMMU 组的典型症状:整屏纯绿](images/rk3566-h264-green.png)

\`\`\`bash
for g in /sys/kernel/iommu_groups/*/devices/*; do
  dev=$(basename $g)
  case "$dev" in *rkvenc*|*rkisp*) echo "$dev";; esac
done
# fdf40000.rkvenc → group 5
# fdff0000.rkisp  → group 7
\`\`\`

正确做法:**在编码器前把缓冲"落回普通内存"**,让编码器走拷贝路径而不是零拷贝。两个条件缺一不可:

1. 加一次真实的格式转换 \`videoconvert ! video/x-raw,format=I420\`(NV12→I420)。**不能写成同格式**,否则 \`videoconvert\` 判定"无需转换"直接透传,缓冲类型没变。
2. 环境变量 **\`GST_MPP_NO_RGA=1\`**。板上的 \`videoconvert\` 默认是 RGA(2D 加速器)版,它转换完会把输出**又导出成 DMABuf**,等于没断链。关掉它,退回软件转换,输出才是真正的普通内存。

> 编码器内部逻辑就是一句话:输入是 DMABuf 就 import(零拷贝),否则就 copy(安全)。我们要做的就是逼它走 copy。

## 七、⑥ 网络传输:把 H.264 码流发出去

H.264 编码后的数据还需要**封装成网络协议**。常见方式:

| 协议 | 传输层 | 延迟 | 适用场景 | 板子侧需要什么 |
|---|---|---|---|---|
| **RTP** | UDP | 低 | 局域网 VLC 播放 | \`rtph264pay\` + \`udpsink\` + SDP |
| **RTSP** | TCP/UDP | 低 | 局域网播放器(更像"摄像头") | 板子上跑 RTSP 服务(mediamtx / gst-rtsp-server) |
| **RTMP** | TCP | 中 | 推送到直播云平台 | \`rtmpsink\` + 云推流地址/推流码 |
| **WebRTC** | UDP | 极低 | 手机浏览器/App 观看 | \`webrtcbin\` + 信令服务器 |

### 7.1 RTP 与 rtph264pay

\`rtph264pay\` 的作用是**把 H.264 拆成 RTP 数据包**(大 NAL 拆成 FU-A 分片,小 NAL 用 STAP-A 聚合,每包带时间戳和序号)。

但要注意:**\`rtph264pay\` 只负责封装,它本身不一定把数据发到网络**。后面还必须有网络输出组件,例如:

- \`udpsink\`
- \`rtspclientsink\`
- \`webrtcbin\`

完整的 GStreamer 概念链路是:

\`\`\`text
v4l2src
 ! video/x-raw,format=NV12,width=1920,height=1080,framerate=30/1
 ! mpph264enc
 ! h264parse
 ! rtph264pay
 ! 网络发送组件(udpsink / rtspclientsink / webrtcbin ...)
\`\`\`

### 7.2 本项目实际用的:RTP + UDP + SDP

考虑到板子是"自己发、电脑收"的简单场景,我们用的是**最轻的裸 RTP/UDP**,配合一份 SDP 告诉 VLC 怎么解:

\`\`\`text
h264parse            → 规范化码流(补 SPS/PPS、对齐 AU)
 ! rtph264pay        → 按 RTP 打包
     pt=96              载荷类型 96(动态类型,靠 SDP 告知是 H.264)
     mtu=1200           单包最大 1200 字节(给 IP/UDP 头留余量,避免 IP 分片)
     config-interval=1  周期性重发 SPS/PPS,方便接收端中途接入
 ! udpsink host=<电脑IP> port=5600 sync=false async=false
\`\`\`

> 裸 RTP 的缺点也很明显:没有"协商"过程,接收端必须手工配 SDP,而且流断了不会自愈。想要"填个 \`rtsp://\` 就能播"的体验,就在板子上跑一个 RTSP 服务器(如 mediamtx),把这条流喂给它。

## 八、HDMI 在哪里?

**HDMI 是另一条输出分支,不是网络传输的一部分。** 可以理解为:

\`\`\`text
ISP 输出 NV12
   ├──► HDMI 显示
   └──► H.264 编码 ──► 网络
\`\`\`

所以理论上可以同时做到:

- 本地 **HDMI 实时预览**
- **网络推流**到电脑或手机

但这样会**增加 RK3566 的整体负荷**(ISP 出两路、VPU 编码、内存带宽都要分摊),做的时候要留意帧率和功耗。

## 九、虚拟机的作用(开发机 ≠ 运行环境)

虚拟机主要用于:

- 编译内核
- 编译设备树
- 编译视频应用
- 检查 SDK 中的 MPP / GStreamer
- (可选)跑一个 RTSP 服务器

真正运行时:

| 角色 | 在哪跑 |
|---|---|
| 视频采集、ISP、编码、推流程序 | **开发板** |
| VLC / 手机播放 | **电脑或手机** |

**虚拟机不是必须参与视频传输的。** 如果确实想让 VM 当服务器,必须保证开发板和 VM 网络互通——VMware 通常用**桥接模式**最简单(NAT 模式下板子访问不到 VM)。

## 十、完整命令(可以直接跑的那一条)

把五环串起来,板子上一条 \`gst-launch-1.0\` 就是完整链路。下面还多了两条"采样支路",用来在板子上客观测亮度和白平衡(不影响主画面):

\`\`\`bash
GST_MPP_NO_RGA=1 gst-launch-1.0 -e \\
  v4l2src device=/dev/video0 \\
  ! video/x-raw,format=NV12,width=1920,height=1080,framerate=30/1 \\
  ! tee name=t \\
  t. ! queue leaky=downstream max-size-buffers=2 \\
     ! videoconvert ! video/x-raw,format=I420 \\
     ! mpph264enc bps=6000000 gop=30 header-mode=each-idr \\
     ! h264parse ! rtph264pay pt=96 config-interval=1 mtu=1200 \\
     ! udpsink host=192.168.1.5 port=5600 sync=false async=false \\
  t. ! queue leaky=downstream max-size-buffers=2 \\
     ! videorate ! video/x-raw,framerate=2/1 \\
     ! videoscale ! video/x-raw,width=320,height=240 \\
     ! videoconvert ! video/x-raw,format=GRAY8 \\
     ! filesink location=/tmp/aeframe \\
  t. ! queue leaky=downstream max-size-buffers=2 \\
     ! videorate ! video/x-raw,framerate=1/1 \\
     ! videoscale ! video/x-raw,width=64,height=48 \\
     ! videoconvert ! video/x-raw,format=RGB \\
     ! filesink location=/tmp/aecolor
\`\`\`

对照总览图,每一段就是一句话:

| 管道片段 | 对应环节 |
|---|---|
| \`v4l2src device=/dev/video0 ! ...NV12...\` | ①+② 从 ISP 主输出取 NV12 |
| \`videoconvert ! I420\` + \`mpph264enc\` | ③ 断链 + 硬件 H.264 编码 |
| \`h264parse ! rtph264pay ! udpsink\` | ④ RTP 封装推流 |
| 电脑端 VLC + SDP | ⑤ 解码显示 |

VLC 用的 SDP:

\`\`\`text
v=0
o=- 0 0 IN IP4 127.0.0.1
s=TSPI Camera H264
c=IN IP4 192.168.1.5
t=0 0
m=video 5600 RTP/AVP 96
a=rtpmap:96 H264/90000
\`\`\`

字段对应关系:

- \`m=video 5600\` —— 监听 5600 端口
- \`RTP/AVP 96\` —— 载荷类型 96,**必须和板上 \`rtph264pay pt=96\` 一致**
- \`a=rtpmap:96 H264/90000\` —— 96 号载荷是 H.264,时钟 90 kHz

也可以命令行验证整条链路:

\`\`\`bash
gst-launch-1.0 udpsrc port=5600 caps="application/x-rtp,payload=96" \\
  ! rtph264depay ! h264parse ! avdec_h264 ! autovideosink
\`\`\`

实测这条管道 1080p 下约 **15~16.5 fps、3~6 Mbps**(瓶颈是软件 NV12→I420 转换),画面如下:

![硬件 H.264 推流解码后的真实画面](images/rk3566-h264-ok.png)

## 十一、建议的实施顺序

由内到外、由简到繁,一步一验证:

1. OV8858 → CSI → ISP(先让 \`dmesg\` 认到 sensor、\`media-ctl\` 链路 ENABLED)
2. ISP 输出 NV12(抓帧看字节数对不对)
3. NV12 保存或抓帧验证(确认不是全黑/全绿)
4. NV12 → 硬件 H.264(写文件,别牵扯网络)
5. H.264 保存成文件并播放(确认编码正确)
6. H.264 → RTP → 电脑 VLC(本项目这一步已通)
7. 局域网稳定后再做手机
8. 最后再接入云(RTMP)或 WebRTC

## 十二、概念速记

| 名词 | 一句话理解 |
|---|---|
| **MIPI CSI** | 摄像头和芯片之间的**硬件传输接口**(只搬数据) |
| **Bayer RAW** | 传感器输出的最原始数据(每像素单色) |
| **ISP** | 把 RAW 处理成可用彩色画面的处理器(去马赛克/3A/缩放) |
| **NV12** | ISP 输出的**原始图像格式**(Y 平面 + UV 平面,未压缩,很大) |
| **H.264 / mpph264enc** | **压缩后适合网络传输**的视频格式(RK3566 用硬件编码) |
| **RTP** | 把 H.264 拆成网络包的封装格式(需要配 \`udpsink\` 等发送端) |
| **RTSP / RTMP / WebRTC** | 不同的**网络传输协议**(局域网 / 推流云 / 低延迟) |

整条链路的本质是**五次形态转换**:

\`\`\`text
光子 →(sensor) Bayer RAW →(ISP+3A) NV12 →(VENC) H.264 →(RTP) UDP 包 →(解码) 画面
\`\`\`

## 十三、常见故障与定位(按现象查)

| 现象 | 多半是哪一环 | 怎么查 |
|---|---|---|
| 白天画面也全黑 | ② ISP 的 3A 没起来 | 先看进程里有没有 \`rkaiq_3A_server\`,没有就起它 |
| 画面一直闪绿 | ② 有两套 3A 在抢 \`/dev/video8\` | 只保留一套 3A |
| 画面偏青绿(缺红) | ② ISP 白平衡参数被改成手动 | 查 IQ 文件 WB 模式,别手改 |
| **整屏纯绿(不是黑)** | ③ 编码器零拷贝读到零页 | 补 \`GST_MPP_NO_RGA=1\` + \`videoconvert ! I420\` |
| 电脑黑屏、板上没有 gst 进程 | ④ 推流进程死了 | 看推流日志;重启推流 |
| 电脑黑屏、板上 gst 还活着 | ⑤ 接收端没接上 / 流断过没自愈 | 关掉播放器重开 SDP |
| dmesg 搜不到 sensor | ① 驱动没编进内核 | 打开对应 \`CONFIG_VIDEO_*\` 重编 |
| 抓帧 0 字节 | ② 链路没 ENABLED | \`media-ctl -p\` 看连接 |

一个很好用的**码流判据**:如果 RTP 负载**平均只有几十字节/包**、且 P 帧大小**完全不变**,基本可断定编码器吃到的是全零输入(回到第六节的零拷贝问题);正常画面满包应在 1150~1388 字节/包。

## 十四、小结

- 采集端只出 RAW,**必须**有 ISP + 常驻 3A,否则没画面
- **MIPI CSI = 硬件传输接口;NV12 = ISP 输出的原始格式;H.264 = 压缩后适合网络传输的格式;RTSP/WebRTC/RTMP = 网络协议**
- 硬件编码要小心**零拷贝跨 IOMMU 组**的坑:断开 DMABuf 链(软件转换 + \`GST_MPP_NO_RGA=1\`)比什么都重要
- HDMI 是 ISP 之后的**另一条输出分支**,和网络推流可以并存,但会增加负荷
- 虚拟机只管**编译/开发**,真正跑链路的是开发板,接收端是电脑/手机
`,
  },
  {
    id: "lvgl-render-pipeline",
    title: "LVGL 学习笔记(一):从三要素到双缓冲,搞懂屏幕是怎么亮起来的",
    date: "2026-08-31",
    tags: ["LVGL", "ESP32", "嵌入式"],
    summary: "在 ESP32-S3 上跑通 LVGL 之后,把渲染管线彻底梳理了一遍:移植三要素、渲染其实是计算而不是搬运、标脏机制、PARTIAL 局部渲染加双缓冲的同步原理,以及我踩过的 DMA 内存和 RGB565 字节序两个坑。",
    content: `
最近在 ESP32-S3 上把 LVGL 跑通了(240x320 的 ST7789 SPI 屏,触摸用的 XPT2046)。跑通之后我盯着代码问了自已一堆问题:渲染到底是什么?缓冲区里装的是什么?为什么我的两块缓冲区没有起到双缓冲的效果?这篇笔记把整条渲染管线串起来。

## 一、LVGL 是什么:它管怎么画,我管画到哪

LVGL 是一个纯 C 写的开源图形库,**不依赖操作系统也不依赖硬件**。它的架构分三层:

\`\`\`text
应用层:  我写的代码 / SquareLine 生成的 UI(创建控件、写事件回调)
核心层:  对象系统 + 渲染引擎 + lv_timer 调度器 + 内存管理
驱动层:  flush_cb 显示回调 / read_cb 输入回调 —— 由我对接硬件
\`\`\`

一句话总结:**LVGL 负责"怎么画",我提供一块 RAM 画布和一个把画布发到屏幕的回调函数,其余全是它的事。**

## 二、移植三要素(缺一不可)

### 1. lv_tick:给 LVGL 一块表

\`\`\`c
static void lv_tick_cb(void *arg)
{
    lv_tick_inc(1);     /* 每 1ms 调一次,ESP32 上用 esp_timer 周期回调 */
}
esp_timer_start_periodic(timer, 1000);   /* 单位微秒 = 1ms */
\`\`\`

动画、按下计时、超时判断全靠它。没有 tick,界面永远静止。

### 2. lv_timer_handler:LVGL 的心跳

\`\`\`c
static void lvgl_task(void *arg)
{
    while (1) {
        uint32_t wait_ms = lv_timer_handler();  /* 处理渲染/动画/输入 */
        vTaskDelay(pdMS_TO_TICKS(wait_ms));     /* 按返回值决定歇多久 */
    }
}
\`\`\`

它内部跑一遍定时器链表:发现脏区域就渲染、推进动画、读取触摸。5~10ms 调一次比较合适。

### 3. flush_cb:把画布发到屏幕

\`\`\`c
void st7789_flush_cb(lv_display_t *disp, const lv_area_t *area, uint8_t *px_map)
{
    /* area 是要刷新的坐标区域,px_map 里是渲染好的像素 */
    st7789_set_window(area->x1, area->y1, area->x2, area->y2);  /* 开窗 */
    lv_draw_sw_rgb565_swap(px_map, w * h);                      /* 字节序交换 */
    /* SPI 发送 px_map ... */
    lv_display_flush_ready(disp);   /* 告诉 LVGL:这块画完了 */
}
\`\`\`

三者关系:**tick 是表,handler 是干活的人,flush_cb 是交货通道。** 只有表没有干活的人,界面不动;只有干活的人没有交货通道,画了也白画。

## 三、渲染不是搬运,是计算

我一开始以为"渲染"就是把图片数据从 flash 搬到缓冲区。错。渲染是 CPU **逐像素计算**出最终颜色:

\`\`\`text
在背景图上画一行字,每个像素要经历:
1. 读背景图在这个位置的颜色(压缩图还要先解码)
2. 查字体点阵:这个位置有没有笔画、透明度多少
3. 混合运算: final = 背景 × (1-α) + 文字颜色 × α
4. 写入 draw buffer
\`\`\`

每一步都是算术运算,不是 memcpy。所以渲染是整条管线里最耗 CPU 的环节,而"搬运"(DMA 发 SPI)反而是便宜的。

## 四、标脏:LVGL 高效的核心

调用 \`lv_label_set_text(label, "12:31")\` 时,LVGL **并不马上画**,只把 label 所占的那一小块区域记成"脏区域"(dirty = 画面过期了)。

\`\`\`text
set_text ──> 标脏(记下区域,瞬间完成)
                │
lv_timer_handler ──> 有脏区域吗?
                     ├─ 没有:直接返回(空闲时几乎零开销)
                     └─ 有:只渲染脏区域 ──> 发送 ──> 清除脏标志
\`\`\`

240x320 全屏是 76800 像素,一个 label 只占 1200 像素——标脏机制让重绘量直接砍掉 98%。手表界面 99% 时间静止,没有这个机制 CPU 早就烧穿了。

## 五、PARTIAL 模式:一块 40 行高的画布刷整屏

我的画布配置:

\`\`\`c
#define BUF_LINES 40
draw_buf_bytes = 240 * 40 * 2;   /* 19200 字节,只有 1/8 屏 */
buf1 = heap_caps_malloc(draw_buf_bytes, MALLOC_CAP_DMA | MALLOC_CAP_INTERNAL);
buf2 = heap_caps_malloc(draw_buf_bytes, MALLOC_CAP_DMA | MALLOC_CAP_INTERNAL);
lv_display_set_buffers(disp, buf1, buf2, draw_buf_bytes, LV_DISPLAY_RENDER_MODE_PARTIAL);
\`\`\`

一帧全屏是 240x320x2 = 150KB,画布只有 18.75KB。PARTIAL 模式下 LVGL 分 8 次渲染加发送,拼出整帧;配合标脏,大多数时候连一整块都刷不满。

**为什么要两块(buf1/buf2)?** 乒乓轮换:CPU 往 buf2 渲染下一块时,DMA 正在把 buf1 发往屏幕,两件事并行。

**同步怎么做?** 不需要预测谁快谁慢,靠一个完成标志:

\`\`\`text
渲染循环规则: 渲染某块画布之前,必须确认它上一次的发送已经完成
  - DMA 发完 buf1 ──> 调 lv_display_flush_ready() 清标志
  - CPU 渲染完 buf2 到交货点,发现标志没清 ──> 等一等
  - 发现标志已清 ──> 直接交货,零等待
\`\`\`

这样同一块缓冲永远不会同时被 CPU 写和 DMA 读,两种时序都天然正确。

## 六、我踩过的两个坑

### 坑 1:DMA 不能访问 PSRAM

ESP32-S3 有 8MB PSRAM,但 SPI 控制器的 **DMA 只能访问内部 SRAM**。画布如果 malloc 在 PSRAM 里,flush 时会传输异常或性能暴跌。所以必须:

\`\`\`c
heap_caps_malloc(size, MALLOC_CAP_DMA | MALLOC_CAP_INTERNAL);
\`\`\`

### 坑 2:RGB565 字节序

ESP32 是小端(低字节在前),SPI 发数据先发低地址字节,但 ST7789 要求**先收高字节**。不交换的话红蓝/明暗全错。发之前调用 \`lv_draw_sw_rgb565_swap()\` 逐像素交换高低字节。

## 七、一个待改进项

我现在的 flush_cb 用的是 \`spi_device_polling_transmit\`(阻塞轮询),发完才返回、马上调 flush_ready——这等于渲染和发送**串行**,双缓冲白配了。正确姿势是 \`spi_device_queue_trans\` 入队即返回,SPI 完成中断里才调 flush_ready,让 CPU 在 DMA 发送期间回去渲染下一块。实测这种异步化一般能提升 20%~40% 帧率,准备下周改。

## 写在最后

整条管线串起来:

\`\`\`text
改数据 ──> 标脏 ──> lv_timer_handler 发现脏区域
       ──> CPU 渲染(计算像素)进 draw buffer
       ──> DMA 搬运经 SPI 写进屏幕 GRAM
       ──> 屏幕驱动电路自己把 GRAM 内容点亮(不占 CPU)
\`\`\`

理解了"渲染是计算、搬运是 DMA、显示是屏幕自己的事"这个分工,后面再看任何 GUI 框架(包括手表 SDK)都是同一套思路。下一篇记录异步 flush 的改造和帧率实测。
`,
  },
  {
    id: "riemann-zeta-continuation",
    title: "黎曼 ζ 函数的解析延拓步骤:从级数到亲手求出非平凡零点",
    date: "2026-08-23",
    tags: ["数学", "复分析", "Python"],
    summary: "黎曼猜想谈论的零点,位于级数根本不收敛的区域——所以得先把 ζ 函数解析延拓出去。从欧拉级数出发,经过欧拉乘积、η 函数、θ 积分一步步延拓到全平面,用二十行 Python 亲手算出前 13 个非平凡零点,讲清 RH 与素数分布的深层联系,最后盘点可能的证明进攻路线。",
    content: `
黎曼猜想是数学里最著名的未解之谜,但它谈论的"零点"位于一个级数根本不收敛的区域——所以在谈零点之前,得先把 ζ 函数**解析延拓**出去。这篇文章记录我从原始级数出发,一路走到亲手算出非平凡零点的完整过程:每一步结论都配了可运行的验证代码。

## 一、出发点:只在半个平面有效的级数

\`\`\`text
ζ(s) = Σ 1/nˢ = 1 + 1/2ˢ + 1/3ˢ + …    (只在 Re(s) > 1 收敛)
\`\`\`

s 是复数,Re(s) 表示实部。级数收敛与否只看实部:|n⁻ˢ| = n^(−Re s),虚部只贡献旋转、不改变大小。在收敛域内还有等价的**欧拉乘积**:

\`\`\`text
ζ(s) = ∏ 1/(1 − p⁻ˢ)    (p 取所有素数)
\`\`\`

麻烦在于:黎曼猜想关心的零点全部位于 0 < Re(s) < 1 的**临界条带**,而级数在那里根本不定义 ζ(s=1 处就是发散的调和级数)。所以第一步:延拓。

## 二、热身:一个玩具例子

\`\`\`text
1/(1−z) = 1 + z + z² + z³ + …    (只在 |z| < 1 内成立)
\`\`\`

三个观察:

- z = 0 处函数值是 1,很乖;真正的爆炸点是 z = 1(分母为零)
- z = 2 处函数值 = −1,但级数 1 + 2 + 4 + 8 + … 发散——**发散的是表达式,不是函数**
- 收敛半径 1 恰好等于中心 0 到奇点 z = 1 的距离(泰勒级数的一般规律)

解析延拓的合法性由**恒等定理**保证:两个解析函数哪怕只在很小的区域上相等,就在整个连通区域上相等。推论:**延拓如果存在,必然唯一**——延拓是"发现"函数的其余部分,不是"发明"。级数只是函数的一张局部照片,照片外函数仍然存在,只是还没被算出来。

## 三、第一块拼图:欧拉乘积怎么来的

每个因子按等比级数展开(公比 p⁻ˢ,你已经会的那个公式):

\`\`\`text
1/(1 − p⁻ˢ) = 1 + p⁻ˢ + p⁻²ˢ + p⁻³ˢ + …
\`\`\`

把素数 2 和 3 的两个括号**逐项相乘**(左边每一项 × 右边每一项,一次不落):

| × | 选 1 | 选 1/3ˢ | 选 1/9ˢ |
|---|---|---|---|
| **选 1** | 1 | 1/3ˢ | 1/9ˢ |
| **选 1/2ˢ** | 1/2ˢ | 1/6ˢ | 1/18ˢ |
| **选 1/4ˢ** | 1/4ˢ | 1/12ˢ | 1/36ˢ |
| **选 1/8ˢ** | 1/8ˢ | 1/24ˢ | 1/72ˢ |

每一格 = 行选项 × 列选项,分母是 2ᵃ × 3ᵇ(用了 aˢ · bˢ = (ab)ˢ 这条运算法则)。把格子全加起来,恰好是"只含 2、3 因子的整数"的倒数 s 次方之和。再乘进 5、7、11… 的括号,由**算术基本定理**(每个整数的素因子分解唯一),每个整数恰好出现一次、一次不漏:

\`\`\`text
∏ 1/(1 − p⁻ˢ) = Σ 1/nˢ    (对所有素数 p 连乘)
\`\`\`

数值验证(s = 2):乘上前 4 个素数得 1.5951,前 25 个得 1.6419,前 1229 个得 1.644917——一路逼近 π²/6 = 1.644934067。这座桥是 ζ 与素数分布一切深刻联系的源头:**整数的世界 = 素数的世界**。

## 四、第二块拼图:η 函数,推到 Re(s) > 0

加正负号能让级数收敛得更宽。最直观的例子:

\`\`\`text
1 + 1/2 + 1/3 + 1/4 + …   →  ∞
1 − 1/2 + 1/3 − 1/4 + …   =  ln 2
\`\`\`

(ln 2 的来历:对 1/(1+x) = 1 − x + x² − … 两边从 0 积到 1,左边积出 [ln(1+x)]₀¹ = ln 2,右边逐项积分得到交错调和级数;积到端点的合法性由 Abel 定理保证。)

定义 η(s) = Σ (−1)ⁿ⁻¹ / nˢ,它对 Re(s) > 0 收敛(正负项互相抵消,驯服发散)。与 ζ 逐项对照做减法:

\`\`\`text
ζ − η = 2·(1/2ˢ + 1/4ˢ + 1/6ˢ + …) = 2¹⁻ˢ·ζ
\`\`\`

偶数项提出 2⁻ˢ 后又变回 ζ 自己,于是 η(s) = (1 − 2¹⁻ˢ)·ζ(s),反解:

\`\`\`text
ζ(s) = η(s) / (1 − 2¹⁻ˢ)    (Re(s) > 0, s ≠ 1)
\`\`\`

在重叠区 Re(s) > 1 上两边逐点相等 → 恒等定理保证:它就是 ζ 在 Re(s) > 0 的唯一真身。实测 s = 0.5(原级数发散区):交错级数部分和逼近 0.6049,除以 1 − √2 得 ζ(0.5) ≈ −1.46035,与 mpmath 的延拓真值吻合。

## 五、第三块拼图:θ 积分,一步到全平面

η 只到 Re(s) > 0,左边还有半平面没有着落。这步的关键想法:**级数没有"把手"可拧**——要么整体收敛要么不收敛;**积分有**——它的端点行为可以逐个修理。

**第一步:又是等比级数!**(你已经掌握的工具第三次登场)

\`\`\`text
1/(eˣ − 1) = e⁻ˣ/(1 − e⁻ˣ) = e⁻ˣ + e⁻²ˣ + e⁻³ˣ + …    (x > 0)
\`\`\`

**第二步:逐项积分,每一项积出 1/nˢ。** 换元 u = nx 可算出 ∫₀^∞ xˢ⁻¹·e⁻ⁿˣ dx = Γ(s)/nˢ,于是:

\`\`\`text
1/Γ(s) · ∫₀^∞ xˢ⁻¹/(eˣ − 1) dx = Σ 1/nˢ = ζ(s)
\`\`\`

数值验证:s = 2 时左边积出 1.644934067,与 π²/6 逐位一致——积分确实等于级数。

**第三步:找到坏点并修理。** 积分在 x → ∞ 处好得很(e⁻ˣ 指数衰减),唯一的坏点在 x → 0:那里 eˣ ≈ 1 + x,所以 1/(eˣ − 1) ≈ 1/x,被积函数 ≈ xˢ⁻²——恰好需要 Re(s) > 1 才可积。**和级数一模一样的门槛,但这次它长在一个具体的位置上,可以动手修。**

修法:把奇性部分显式减掉、显式积出来——

\`\`\`text
ζ(s) = 1/Γ(s) · ∫₀¹ xˢ⁻¹·( 1/(eˣ − 1) − 1/x ) dx    ← 减掉 1/x 后,收敛区扩到 Re(s) > 0
     + 1/Γ(s) · ∫₁^∞ xˢ⁻¹/(eˣ − 1) dx                ← 全平面收敛
     + 1/Γ(s) · 1/(s − 1)                             ← ∫₀¹ xˢ⁻² dx 的显式值
\`\`\`

减掉 1/x 后赚到 Re(s) > 0;再减掉 −1/2、x/12、……每多减一项多赚一条带,减完所有项就覆盖全平面,且极点只剩显式露出的那个 s = 1。这就是解析延拓的全部手艺:**把发散集中到一个局部位置,减掉、单独积出、剩下的收敛区变大。**

**黎曼 1859 年的做法(一次到位版)**:他不是一项一项减幂级数,而是把积分换成 θ 函数版本,用模变换 θ(t) = t^(−1/2)·θ(1/t) 把 t → 0 端整个翻转到 t → ∞(那里天然收敛),一步得到全平面公式,而且被积函数天然对称。记 ψ(t) = Σ e^(−πn²t):

\`\`\`text
Λ(s) := π^(−s/2)·Γ(s/2)·ζ(s)
      = ∫₁^∞ ψ(t)·( t^(s/2−1) + t^((1−s)/2−1) ) dt + 1/(s·(s−1))
\`\`\`

右边的积分对**一切复数 s** 都收敛。于是:ζ 延拓到全平面,唯一极点是 s = 1(残数 1)。

而且右边在 s ↔ 1−s 下不变,顺手白送**函数方程**:

\`\`\`text
Λ(s) = Λ(1−s)
⟺ ζ(s) = 2ˢ·πˢ⁻¹·sin(πs/2)·Γ(1−s)·ζ(1−s)
\`\`\`

两个记号说明。公式里的 \`:=\` 读作"定义为":Λ 是给 π^(−s/2)·Γ(s/2)·ζ(s) 这个反复出现的组合**起的名字**(左边新名字、右边是含义),第二行的等号才是推导出的定理。Λ 的中文名叫**完成 ζ 函数**——"完成"指的是把 ζ 配平成对称的版本:对称性长在 Λ 身上,不在 ζ 身上(ζ(s) = ζ(1−s) 并不成立)。再进一步,用 ½·s·(s−1) 把 Λ 在 0 和 1 处的两个极点抹掉,就得到黎曼原文使用的 ξ 函数:

| 符号 | 定义 | 中文名 | 性质 |
|---|---|---|---|
| ζ(s) | Σ 1/nˢ | 黎曼 ζ 函数 | 极点 s = 1,不对称 |
| Λ(s) | π^(−s/2)·Γ(s/2)·ζ(s) | 完成 ζ 函数 | Λ(s) = Λ(1−s),在 0、1 处有极点 |
| ξ(s) | ½·s·(s−1)·Λ(s) | 黎曼 ξ 函数 | 整函数,ξ(s) = ξ(1−s) 完美对称 |

用 ξ 陈述 RH 最干净:**ξ(s) 的所有零点都在临界线 Re(s) = 1/2 上。**

顺带两个著名特值:ζ(0) = −1/2,ζ(−1) = −1/12。级数在 s = −1 发散,但延拓后的函数在那里取有限值——这些值是解析性逼出来的唯一可能,不是人为约定。

## 六、函数方程送出的零点地图

把函数方程写成显式形式,对右边的四个因子逐个审查"造零能力":

\`\`\`text
ζ(s) = 2ˢ·πˢ⁻¹ · sin(πs/2) · Γ(1−s) · ζ(1−s)
\`\`\`

- 2ˢ·πˢ⁻¹:指数函数,处处非零
- sin(πs/2):在负偶数 s = −2, −4, −6, … 处为零——**唯一的"造零工厂"**
- Γ(1−s):只有极点、不贡献零
- ζ(1−s):当 s < 0 时 1−s > 1,落在欧拉乘积的领地里,非零

于是负偶数全是零点(平凡零点),负奇数一个都不是(ζ(−1) = −1/12 就是一例)。零点地图:

| 区域 | 零点情况 | 原因 |
|---|---|---|
| Re(s) > 1 | 无零点 | 欧拉乘积每个因子非零 |
| Re(s) < 0 | 恰好 −2, −4, −6, …(平凡零点) | sin 因子在负偶数为零 |
| 0 ≤ Re(s) ≤ 1 | 其余全部零点(非平凡零点) | RH 的研究对象 |

**黎曼猜想**:所有非平凡零点都落在临界线 Re(s) = 1/2 上。1/2 这条线之所以特殊,正因为它是函数方程 s ↔ 1−s 这面镜子的镜面所在。

## 七、动手求解非平凡零点:六步算法

**第一步:把复值函数转成实值。** ζ(1/2 + it) 是复值函数,实部虚部都在振荡,没法直接用"变号"夹零点。乘一个模长恒为 1 的旋转因子,把它转成**实值**的 Hardy Z 函数:

\`\`\`text
Z(t) = e^(iθ(t))·ζ(1/2 + it)
θ(t) = Im ln Γ(1/4 + it/2) − (t/2)·ln π
|e^(iθ)| = 1  ⇒  Z(t) = 0  ⟺  ζ(1/2 + it) = 0
\`\`\`

于是"找零点"变成"找实值曲线穿越横轴的位置":

![Hardy Z 函数曲线](images/z-function.png)

![zeta 在临界线上的模长](images/zeta-line.png)

**第二步:算 Z 很便宜。** 计算 Z(t) 用 Riemann–Siegel 公式:Z(t) = 2·Σ cos(θ(t) − t·ln n)/√n + R(t)(n 从 1 到 N),其中 N = ⌊√(t/2π)⌋——算 t = 60 处只需 3 项。这个效率是能验证 10¹³ 个零点的根基。

**第三步:先对账单,知道该找几个。** 零点个数有公式可查(Riemann–von Mangoldt):N(T) ≈ (T/2π)·ln(T/2π) − T/2π + 7/8。代入 T = 60 得 12.87,期望约 13 个——扫描结束后用它核对,一个都没漏。

**第四步:小步长扫描,夹住零点。** 从 t = 0.1 出发、以 0.25 为步长逐点计算 Z(t)。相邻两点一旦异号,由实值连续函数的**介值定理**,区间内必有零点。以第一个零点为例(Z 从 Z(0) = ζ(1/2) ≈ −1.46 出发,一直保持负号):

\`\`\`text
Z(14.10) ≈ −0.027 < 0,  Z(14.35) ≈ +0.174 > 0   →  零点被夹在 (14.10, 14.35)
\`\`\`

步长 0.25 是安全的:这个高度上相邻零点的最小间距约 1.77,不会一步跨过两个零点。

**第五步:二分法收缩。** 每次取区间中点、看 Z 的符号、保留异号的那一半——区间宽度每步减半,80 次后宽度 < 0.25/2⁸⁰ ≈ 10⁻²⁵,精度直接拉满。第一个零点前四步的真实轨迹:

\`\`\`text
区间 (14.1000, 14.3500)   宽 0.25
中点 14.2250 : Z > 0  →  (14.1000, 14.2250)
中点 14.1625 : Z > 0  →  (14.1000, 14.1625)
中点 14.1313 : Z < 0  →  (14.1313, 14.1625)
中点 14.1469 : Z > 0  →  (14.1313, 14.1469)
…… 80 次后收敛到 14.13472514173469379046
\`\`\`

对 T = 60 把六步完整跑一遍,13 个零点全部落网:

| 序号 | 求得的 t | 序号 | 求得的 t |
|---|---|---|---|
| 1 | 14.13472514173469379 | 8 | 43.32707328091499952 |
| 2 | 21.02203963877155499 | 9 | 48.00515088116715973 |
| 3 | 25.01085758014568876 | 10 | 49.77383247767230218 |
| 4 | 30.42487612585951321 | 11 | 52.97032147771446064 |
| 5 | 32.93506158773918969 | 12 | 56.44624769706339480 |
| 6 | 37.58617815882567126 | 13 | 59.34704400260235308 |
| 7 | 40.91871901214749519 | | |

**第六步:验证。** 每个结果的实部都精确等于 1/2,与 mpmath.zetazero 逐位一致,总数与第三步的账单吻合。再直接代回 ζ 做两个"在线"检查:

- |ζ(0.5 + 14.1347i)| ≈ 10⁻²⁵,就是零
- |ζ(0.6 + 14.1347i)| ≈ 0.076,实部只挪 0.1 就立刻离开零点——"恰好在线上"非常特殊

**把验证做严格:三层递进。** "算出来很小"永远不等于"恰好为零"——解析延拓后的 ζ 没有封闭公式,任何数值都带误差。怎么把一个候选点变成被严格确认的零点?分三层:

**第一层:点检查。** 直接算 |ζ(s₀)|:

\`\`\`text
s₀ = 1/2 + 14.13472514…i :  |ζ(s₀)| = 6.7×10⁻¹⁶    (强烈暗示是零)
s₁ = 0.6  + 14.13472514…i :  |ζ(s₁)| = 7.6×10⁻²    (明显不是零)
\`\`\`

小到机器精度是好证据,但逻辑上一个函数值可以"恰好很小而不为零"——这只配当初筛。

**第二层:牛顿精化。** 在 s₀ 附近做迭代 s ← s − ζ(s)/ζ′(s),若附近真有零点,迭代会二次收敛把它"吸"出来:实测从粗值 14.13 出发,迅速稳定在 0.5 + 14.13472514173469379045725…i,该处 |ζ| = 1.1×10⁻³⁷。收敛行为本身就是第二重证据(附近没有零点的点,牛顿要么发散要么漂走)。

**第三层:辐角原理——唯一严格的一层。** 思路极其漂亮:别盯着零点本身,去数它周围的"圈数"。取小方块 B 围住候选点,ζ 把方块边界映成一条闭合曲线,辐角原理给出:

\`\`\`text
N = (1/2πi)·∮ ζ′(s)/ζ(s) ds = ζ(∂B) 绕原点的圈数 = B 内零点个数(计重数)

方块A [0.45,0.55]×[14.08,14.18]i(围住 s₀): 绕数 = 1 → 恰好一个零点,s₀ 确认
方块B [0.55,0.65]×[14.08,14.18]i(围住 s₁): 绕数 = 0 → 没有零点,s₁ 否定
\`\`\`

看图最直观:左图(方块A 的边界像)把原点裹了一圈,右图(方块B)把原点漏在外面:

![方块边界的像与绕数](images/winding-demo.png)

两个妙处:一是**只用边界上的 ζ 值**——边界离零点有安全距离(实测边界上最小 |ζ| = 0.039,远大于计算误差),"零点处算不准"的死结被彻底绕开;二是**圈数是整数**,1 和 0 之间隔着鸿沟,不存在模棱两可的圈数——顺带还给出零点的重数(绕数 1 = 单零点,顺带检验了"所有非平凡零点都是单的"这个附带猜想)。

这正是历史上真实使用的验证方法:图灵(1953)第一个把它系统化,现代对前 10¹³ 个零点的验证骨架仍是"辐角原理数出总数 + 线上逐个定位 + 两侧对账"。第三步的账单对数,就是这台机器的平民版。

一句话总结三层:**|ζ| 小(初筛)→ 牛顿收敛(精化)→ 绕数 = 1(严格确认)。**

完整代码(不到 30 行):

\`\`\`python
import mpmath as mp
mp.mp.dps = 25

Z = mp.siegelz   # mpmath 内置的 Hardy Z 函数

def find_zeros_up_to(Tmax, step=mp.mpf("0.25")):
    zeros, t_prev, z_prev = [], mp.mpf("0.1"), Z(mp.mpf("0.1"))
    while t_prev < Tmax:
        t_next, z_next = t_prev + step, Z(t_prev + step)
        if z_prev * z_next < 0:          # 变号: 夹住一个零点
            a, b, fa = t_prev, t_next, z_prev
            for _ in range(80):          # 二分收缩
                m = (a + b) / 2
                if fa * Z(m) <= 0:
                    b = m
                else:
                    a, fa = m, Z(m)
            zeros.append((a + b) / 2)
        t_prev, z_prev = t_next, z_next
    return zeros

zeros = find_zeros_up_to(60)
print("找到零点数:", len(zeros))          # 13
for i, t in enumerate(zeros, 1):
    print(i, mp.nstr(t, 18))
\`\`\`

## 八、RH 与素数分布:证明它到底为了什么

黎曼 1859 年那篇论文的标题就是《论小于给定数值的素数个数》——他研究 ζ 的延拓和零点,全部目的就是数素数。这一节讲清这条链路。

**素数分布的问题怎么提。** 计数函数 π(x) = 不超过 x 的素数个数,是一条每遇素数就跳 1 的阶梯。素数定理(1896)给出两个近似,一粗一精:

\`\`\`text
π(x) ~ x/ln x                      (粗)
π(x) ~ Li(x) = ∫ dt/ln t (从 2 积到 x)   (精)
\`\`\`

实际到 x = 10⁶:π(x) = 78498,Li(x) 的误差只有约 130,而 x/ln x 偏差上万——关键问题从此变成:**Li(x) 的误差有多大、由什么决定?**

![pi(x) 与它的两个近似](images/primes-counting.png)

**显式公式:零点直接写在素数计数公式里。** 对 π(x) 做等价的技术替换 ψ(x) = Σ (pᵏ ≤ x 的 ln p),黎曼得到的显式公式是:

\`\`\`text
ψ₀(x) = x − Σ_ρ x^ρ/ρ − ln(2π) − ½·ln(1 − x⁻²)
\`\`\`

第一项 x 是光滑主项;第二项对 ζ 的**所有非平凡零点 ρ 求和**——素数分布的全部不规则性,被精确编码在零点里。下图:灰阶梯是真实的 ψ(x)(每个素数幂跳一下),红线只用了前 40 个零点的贡献,阶梯形状已经浮现——零点就是构成素数阶梯的原材料:

![零点重构素数阶梯](images/primes-staircase.png)

**每个零点是一个振荡分量,实部决定振幅。** 把单个零点 ρ = β + iγ 的贡献拆开:

\`\`\`text
x^ρ/ρ = x^β · e^(iγ·ln x) / (β + iγ)
        振幅 ~ x^β      频率 = γ(以 ln x 为"时间")
\`\`\`

虚部 γ 是振荡频率,实部 β 是振幅增长率——**决定误差量级的唯一参数**。记 Θ = 所有零点实部的上确界,则素数定理的误差恰好是 x^Θ 量级,于是:

\`\`\`text
RH ⟺ Θ = 1/2 ⟺ π(x) = Li(x) + O(√x·ln x)
\`\`\`

- 已知 Θ ≥ 1/2(Hardy 定理:临界线上有无穷多零点)
- 已知 Θ < 1(素数定理的本质就是 Re(s) = 1 上无零点)
- **RH 就是赌 Θ 取到理论下界 1/2**——素数分布所能达到的最均匀程度

下图:红线是 RH 下的误差天花板 √x·ln x/(8π)(Schoenfeld 定理),到 10⁷ 为止实际误差一直被压在包络之下、贴着 √x 的量级走:

![误差与 RH 预言的包络](images/primes-error-envelope.png)

(不依赖 RH 的最优误差界只有 O(x·e^(−c·√ln x)),比 √x 大得多——这正是"没证明"与"有证明"的差距。)

**一组等价说法(RH 的不同面具):**

| RH 等价于 | 含义 |
|---|---|
| π(x) = Li(x) + O(√x·ln x) | 素数计数的误差恰好 √x 级 |
| ψ(x) = x + O(√x·ln²x) | 切比雪夫函数版 |
| ∣M(x)∣ ≤ x^(1/2+ε) 对任意 ε > 0 | 默滕斯函数同样被 √x 压住 |
| 相邻素数间隙 = O(√p·ln p) | 素数不会长期"集体失踪" |

**如果 RH 是假的会怎样?** 只要存在一个 β > 1/2 的零点,误差就会无穷多次达到 x^β 量级:素数分布比我们相信的更不均匀,Li(x) 会以更大的幅度反复越过 π(x)。(Littlewood 早已证明 π(x) − Li(x) 无穷次变号,但第一个变号点大到 10³¹⁶ 量级;RH 若假,变号会更早、更剧烈。)

**给工程师的类比:素数是信号,零点是频谱。** 把 ln x 当作时间轴,ψ(x) 的阶梯是信号,显式公式就是它的傅里叶级数:主项 x 是直流分量,每个零点贡献一个频率为 γ 的振荡。RH 说的是**所有频率分量的衰减率完全相同**(都是 x^(1/2));一个"线外"零点就像频谱里冒出一个衰减慢得多的分量,误差将被它长期主导。素数分布的随机涨落与 ζ 的频谱结构,是同一件事的两个视角——这正是 RH 在数学里地位如此核心的原因:它是对"素数有多像随机数"的终极定量陈述。

## 九、如果让你来证明:可能的进攻路线

**先认清一个不对称:证伪远比证明容易。** 证伪只需一个反例——找到一个 β ≠ 1/2 的零点,代入 ζ 做高精度确认,RH 当场死亡;证明则要一句话管住无穷多个零点。目前前 10¹³ 个零点已验证全在线上,反例若存在,要么极高,要么根本不存在。

还有一个警告,它决定证明能用什么武器:**RH 的某些自然推广是假的**。Davenport–Heilbronn 构造过一个没有欧拉乘积的狄利克雷级数,同样满足函数方程,但零点明确跑到了线外。任何证明都必须用到 ζ 特有的、那个构造所没有的东西——头号嫌疑就是欧拉乘积(背后是素数分解的唯一性,一种"正性")。

**思路一:谱方法——给零点找一个自伴算子(最被看好的一条路)。** 第八节说过:素数是信号,零点是频谱。Hilbert–Pólya 猜想把这个类比推到极致:

\`\`\`text
存在自伴算子 H,使零点虚部恰是它的本征值:  H·ψₙ = γₙ·ψₙ
\`\`\`

为什么这等于证明 RH?线性代数的基石定理:**自伴算子的本征值全是实数**。γₙ 是实数 ⟺ 零点 1/2 + γₙ·i 落在临界线上。整个猜想被压缩成一句话:零点频谱背后藏着一个"实物理系统"。而且证据不是空想:Montgomery 证明零点间距服从随机厄米矩阵(GUE)本征值的统计分布,Odlyzko 的大规模数值严丝合缝——零点表现得就像某个量子混沌系统的能级。Berry 与 Keating 猜算子可能是极简的 H = x·p(位置 × 动量),Connes 给出非交换几何版本;卡壳处在于,没人能把 x·p 改造成严格对应 ζ 的算子(这条最被看好的路线,下一节详细展开)。

**思路二:正性方法——把显式公式变成不等式。** Weil 判据:RH ⟺ 对一大类测试函数 f,某个对素数求和的量恒非负:

\`\`\`text
Σₚ (ln p/√p)·f(ln p) + (零点侧的对应项) ≥ 0   对所有 f 成立 ⟺ RH
\`\`\`

也就是 RH 本质是一个**正性命题**。Weil 用这条路在有限域上证明了曲线版的 RH,Deligne(1974)推广到高维(Weil 猜想)——20 世纪数学的胜利。但整数的 ζ 没有现成"曲线"可用,于是有了 Deninger 纲领与"F₁ 几何"(一个元素的域):虚构一个 Spec Z 的几何形体,让 Weil 的证明照搬过来。这个几何至今无人造出。

**思路三:矩与随机矩阵——先拿 Lindelöf。** ζ 在临界线上的大小由矩 ∫|ζ(1/2 + it)|^(2k)dt 控制,Keating–Snaith 用随机矩阵理论猜出了全部矩的公式;严格化它们会大幅加强临界线上的控制。离 RH 还隔一层,但每一步都是真定理。

**这条路的前世今生。** 上面的思路全部有真人真事,排成一张表:

| 路线 | 谁、什么时候 | 走到了哪 |
|---|---|---|
| 谱方法猜想 | Hilbert(1910s)、Pólya(1914) | Berry–Keating(1999)的 H = x·p、Connes(1996–99)的非交换几何,均卡在"造不出严格对应 ζ 的算子" |
| 零点统计 | Montgomery(1973)、Dyson 认出 GUE、Odlyzko(1980s) | 是"证据"不是"证明",但指引着谱方法 |
| 正性判据 | Weil(1952) | 同思路在有限域上成功:Weil(1940s)曲线版 RH、Deligne(1974)高维版,后者获菲尔兹奖 |
| F₁ 几何 | Deninger(1990s)、Connes–Consani(2000s 至今) | 核心障碍未破:整数世界缺一个能照搬 Weil 证明的几何形体 |
| 矩方法 | Hardy–Littlewood(1918)起手、Keating–Snaith(2000) | 低阶矩已严格化,一般矩仍是猜想 |
| 等价变形 | Bombieri–Lagarias(1995)等 | 把 RH 翻译成 λₙ ≥ 0 等条件,正性本身仍证不出 |

也有著名的失败,它们同样是这条路的真实部分:Atiyah(2018 年海德堡讲演,框架而非完整证明,未被接受,数月后去世)、de Branges(1980s 起三十多年反复宣称,每一版都被找出漏洞,但其 de Branges 空间理论本身有价值),以及每年 arXiv 上的若干"证明"。一位拿过菲尔兹奖与阿贝尔奖的巨匠选择用它收尾,足见这个问题的诱惑力。

**如果"不能证明"呢?——一个逻辑彩蛋。** RH 等价于"高度 T 以下无线外零点"的一组命题,而每个 T 都可以机械检验。推论:假如 RH 是假的,它的假**必定可被证明**(把反例摆出来即可)。所以如果哪天真的证明了"RH 独立于 ZFC 公理系统",那么——因为公理系统是可靠的——RH 反而必须是真的:"不可判定"恰恰意味着永远抓不到反例。

**赌注押在哪?** 谱方法最值得押:它与全部已知图景严丝合缝——函数方程给出对称轴,显式公式说零点是素数的频谱,GUE 统计说频谱像能级,最后一块拼图就是"能级背后的算子",而"自伴 ⟹ 实谱"是数学里最可靠的把戏之一。历史上费马大定理正是靠椭圆曲线这座跨领域的桥拿下的;ζ 与量子能级之间的桥,就是最像样的下一座。还有一点值得记住:160 年的"失败"并不白费——素数定理、零点密度定理、随机矩阵理论、甚至量子混沌的一部分,都是攻 RH 的副产品。攻城的人没赢,但壕沟里挖出的土,铺成了一整片城区。

## 十、谱方法详解:给零点找一件乐器

**"谱"是什么。** 谱 = 一个算子的全体特征值的集合。词源是光谱:白光过棱镜分解成一组离散频率;氢原子光谱也是一组离散谱线,量子力学后来解释了——每条谱线对应一个能量本征态,能量是哈密顿算子的特征值:

\`\`\`text
H·ψₙ = Eₙ·ψₙ        全体能级 Eₙ = 这个系统的"谱"
\`\`\`

最朴素的版本:两端固定的弦,振动频率只能取基频的整数倍——那就是弦的谱。谱就是系统"天生允许的那些振动方式"。第八节说过素数是信号、零点是频谱,那里还只是修辞;谱方法要把它变成字面意义的等价命题。

**数学引擎:自伴 ⟺ 谱全实。** 线性代数里最可靠的定理之一:

\`\`\`text
H = H†(自伴/厄米,等于自己的共轭转置)  ⟹  H 的所有特征值都是实数
\`\`\`

原因一句话:⟨H·ψ, ψ⟩ = ⟨ψ, H·ψ⟩ 强迫 E·‖ψ‖² 是实数。这不是小定理——它是量子力学"能量必须取实数"的数学根基。

**一张图看懂全部思想。** 取两个 60×60 的随机矩阵:一个做成对称(自伴),一个不做任何限制,各自画出特征值:

![自伴与一般矩阵的谱](images/spectrum-demo.png)

蓝点(一般矩阵)散落在复平面上;红点(自伴矩阵)被一条无形的力死死钉在实轴上——实测虚部精确为 0。**谱方法想做的事,就是把黎曼零点变成红点。**

**Hilbert–Pólya 提议。** 零点长着 1/2 + γₙ·i 的样子,虚部 γₙ 是一串实数。于是提议:

\`\`\`text
存在一个(尚未找到的)自伴算子 H,其特征值恰为全体 γₙ:
H·ψₙ = γₙ·ψₙ
⟹ 每个 γₙ 是实数 ⟺ 每个零点落在 1/2 + γₙ·i ⟺ RH 得证
\`\`\`

注意这个证明的结构之美:不需要逐个检查无穷多个零点(查不完),只需要找到那个算子并验证它自伴——**一句话管住全部**。

**示波器类比。** 你在示波器上看到一串谱线(零点)。谱方法问的是:"什么样的电路能产生、且只能产生这串谱线?"找到电路图(算子),验证电路能量守恒(自伴性),谱线全在实轴上就是必然——不用一根根去量。

**为什么觉得有戏:统计指纹。** 如果零点真是某个算子的谱,间距统计就应该像谱。1972 年普林斯顿的一次茶话上,Montgomery 把自己刚证出的零点间距公式讲给物理学家 Dyson 听,Dyson 当场认出:这是随机厄米矩阵(GUE)本征值的间距公式。Odlyzko 后来算过上亿个零点,统计曲线与 GUE 理论重合——零点的行为方式,恰好就是"量子混沌系统能级"的行为方式。指纹对上了,就差找到这个人。

**卡在哪一百年。** 从谱反推算子是数学里著名的逆问题——"你能听见鼓的面积,却未必听出鼓的形状"。手里只有零点这串"录音",要凭空造出那件"乐器",而且它还得恰好自伴。最接近的尝试:

| 尝试 | 思路 | 现状 |
|---|---|---|
| Berry–Keating(1999) | 猜 H = x·p(位置 × 动量,最简单的自伴标度算子) | 确实产生对数能级,与零点粗像,细节对不上 |
| Connes(1996–99) | 非交换几何痕迹公式,零点以"吸收谱"形式出现 | 框架漂亮,证明所需的额外结构凑不齐 |
| 各种量子化修补(Sierra 等) | 对 x·p 做各种形变与限制 | 至今差一口气 |

**一句话总结:谱方法 = 把"零点在临界线上"翻译成"某个算子的谱是实的",再用"自伴 ⟹ 实谱"一锤定音。**

## 写在最后

整条流水线串起来:

\`\`\`text
Σ 1/nˢ  (Re > 1)
  → η 交错级数 (Re > 0)          [正负抵消]
  → θ 积分 (全平面) + 函数方程    [翻转坏端]
  → sin 因子给平凡零点, 临界条带装非平凡零点
  → Z(t) 实值化, 变号 + 二分 → 1/2 + 14.1347…i
\`\`\`

回头看,整条链的"抓手"其实只有几个基本功:**等比级数三次登场**(欧拉乘积、η 推导、积分表示)、**交错抵消**(驯服发散)、**减奇性**(修积分坏点)、**模变换**(翻转坏端)、**sin 因子**(白送平凡零点)、**实值化夹逼**(把复平面上的零点变成曲线穿越横轴)。

数学上,前 10¹³ 个非平凡零点已验证全部落在 Re(s) = 1/2 上,但"全部"至今无人能证——这正是黎曼猜想。而这条链路最迷人的地方是:**每一步都可以用二十行以内的代码亲手验证**,数学不再是纸上的符号,而是能跑、能对答案的工程。
`,
  },
  {
    id: "esp32-devboard",
    title: "ESP32-S3 开发板入门:从零画一块自己的开发板",
    date: "2026-08-23",
    tags: ["ESP32", "硬件设计", "PCB"],
    summary: "从原理图模块化开始,记录我用嘉立创EDA专业版画 ESP32-S3 开发板的完整过程:电源、USB转串口、自动下载电路,以及那些踩过的坑。",
    content: `
最近在画一块属于自己的 ESP32-S3 开发板。主控选了 **ESP32-S3-WROOM-1-N16R16V**(16MB Flash + 16MB PSRAM),外围电路其实都是成熟方案,但真正动手画一遍,理解完全不一样。

## 整体架构

一块最小系统的 ESP32 开发板,核心就五个模块:

- **电源模块**:USB 5V 输入,AMS1117-3.3 稳压到 3.3V
- **USB 转串口**:CH340C,负责和电脑通信
- **自动下载电路**:两只 NPN 三极管,经典 RTS/DTR 控制 EN/IO0
- **按键**:EN 复位键 + BOOT 下载键
- **扩展排针**:把 GPIO 全部引出来

## 原理图要模块化

第一版原理图被吐槽"太乱了"——元件随手放,有的甚至画到图框外面。正确的做法是**按功能分区**:

> 电源一个框、串口一个框、MCU 一个框,每个模块加上边框和标题,模块之间用网络标签(3V3、GND、EN、IO0)连接,而不是拉一堆长导线。

这样图纸的可读性会好非常多,后期查错也快。

## 双 USB 口的取舍

我一开始贪心画了两个 USB-C:一个 CH340C 串口口,一个 CH340N。后来才知道**两个 CH340 的 TX 同时接在 MCU 的 RX 上,两个口同时插电时会总线冲突**。常见的做法是:

1. 只留一个 CH340 做下载口
2. 第二个口接 ESP32-S3 的原生 USB(GPIO19/20),速度更快还支持 HID

## 坐标单位这个坑

嘉立创EDA 里原理图和 PCB 的坐标单位不一样:

| 域 | 单位 |
|---|---|
| PCB | 1 mil |
| 原理图 | 10 mil |

用错的话,元件会放到 10 倍远的地方去。

下一篇文章写 PCB 布局的思路,敬请期待。
`,
  },
  {
    id: "python-notes-kwargs",
    title: "Python 学习笔记:从一个 headers 字典到 **kwargs",
    date: "2026-08-22",
    tags: ["Python", "学习笔记"],
    summary: "从调用 API 时的 headers 字典出发,一路搞懂字典嵌套列表、链式取值、元组的逗号陷阱,最后打通 **kwargs 和解包赋值。",
    content: `
这周在读 API 示例代码时被一行代码卡住了:

\`\`\`python
prediction_id = generate_result["data"]["id"]
\`\`\`

顺着这行代码,把 Python 的几个基础概念彻底搞明白了,记个笔记。

## 字典的值可以是任何东西

\`\`\`python
data = {
    "model": "seedance-2.5",
    "reference_images": [
        "https://example.com/1.jpg",
        "https://example.com/2.jpg",
    ],
}
\`\`\`

字典套列表、列表套字典,层层嵌套——这不是语法混乱,这就是 **JSON 的 Python 写法**。\`json.dumps(data)\` 一转,就能直接当请求体发出去。

## 链式取值从左往右剥

\`result["data"]["id"]\` 的求值过程:

1. \`result["data"]\` 先取出内层字典
2. \`["id"]\` 再从内层字典里取值

不管嵌套多少层都是这个规则。稳妥写法用 \`.get()\`:

\`\`\`python
prediction_id = generate_result.get("data", {}).get("id")
\`\`\`

取不到得到 \`None\` 而不是直接崩,方便先判断再处理。

## 元组是由逗号定义的

最容易踩的坑:

\`\`\`python
a = ("x", "y")   # 元组,两个元素
b = ("x" "y")    # 字符串自动拼接,得到 "xy"
c = ("xy",)      # 单元素元组,逗号不能省
\`\`\`

**判断标准:看逗号,不看括号。**

## **opts 不是指针

C 语言的 \`**\` 是二级指针,Python 里完全是另一回事:

- \`def f(**opts)\` —— 把多余的关键字参数**打包**成字典
- \`f(**d)\` —— 把字典**摊开**成关键字参数
- \`2 ** 10\` —— 乘方

一收一放,互为逆操作。读 pymavlink 源码时看到它把连接字符串里的 \`baud=57600\` 解析出来塞进 opts 字典,和关键字参数合并处理,设计得挺优雅。

## if __name__ == "__main__"

\`__name__\` 是 Python 自动填的变量:直接运行时等于 \`"__main__"\`,被 import 时等于模块名。所以这段代码的意思是:**只有直接运行本文件才执行,被导入时不执行**。让一个文件既能当脚本跑、又能当模块被复用。

接下来准备拿这些知识写一个调用视频生成 API 的完整脚本:提交任务 → 轮询状态 → 下载视频。
`,
  },
  {
    id: "ai-video-overview",
    title: "AI 视频生成初探:Seedance 和它的对手们",
    date: "2026-08-20",
    tags: ["AI", "视频生成", "Seedance"],
    summary: "想用 Seedance 的参考图生视频功能把自己旅行照片变成奇幻短片,顺手把 2026 年的 AI 视频格局和技术原理整理了一遍。",
    content: `
最近想搞个好玩的事情:用 AI 视频模型的"参考图生视频"功能,把博物馆旅行照片变成一段"壁画苏醒"的奇幻短片。顺便把现在的 AI 视频技术梳理了一遍。

## 底层原理:扩散模型 + Transformer

现在主流视频模型的技术骨架高度趋同,大致三步:

1. **压缩**:用 3D VAE 把视频在空间和时间两个维度压成低维潜空间,运算量降几个数量级
2. **去噪生成**:从纯噪声出发,Transformer 以提示词(和参考图)为条件一步步去噪
3. **解码**:潜空间还原回像素,输出视频帧

图生视频(I2V)是实用性最强的模式——给一张图,模型让它动起来,可控性远高于纯文生视频。

## 2026 年的格局

- **Seedance 2.0**(字节):图生视频最强,即梦 AI 背后的引擎
- **Sora 2**(OpenAI):画面细节和真实感领先
- **Veo 3.1**(Google):音画同步、物理真实感最好
- **Kling 3.0**(快手):长视频、社交短片见长

## 提示词的写法

实测下来,效果稳定的提示词都遵循这个结构:

> 主体+服装 → 动作 → 环境变化 → 镜头语言 → 光影 → 整体风格

比如我的"壁画苏醒"提示词:

\`\`\`text
幽暗的展馆长廊里,黑色长直发的年轻女孩缓步走向一幅巨大的
民族风格壁画……她停下脚步抬头凝视,画中人物的眼睛忽然缓缓
亮起,飞天的飘带破壁而出……镜头从女孩背后缓慢推近至侧脸,
再环绕上升。电影质感,奇幻光影,色彩浓烈饱和。
\`\`\`

要点:**一次一镜**,别贪心写多镜头剪辑;时长先生成 5 秒验证,满意再拼。

## 一些实用关键词

想让画面更梦幻,可以在结尾加这些词:\`轻微的慢动作\`、\`浅景深\`、\`体积光\`。

等视频生成好了,下一篇写个 Python 脚本把"提交任务 → 轮询 → 下载"全流程自动化。
`,
  },
];
