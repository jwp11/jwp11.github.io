/* ============================================
   文章数据文件 —— 在这里新增/修改你的文章
   格式说明:
     id:      文章唯一标识(英文数字,用于网址 ?id=xxx)
     title:   标题
     date:    发布日期
     tags:    标签数组
     summary: 摘要(首页卡片显示)
     content: 正文,支持轻量 Markdown 语法:
              # ## ### 标题 | **粗体** | *斜体* | `行内代码`
              ```语言 代码块``` | > 引用 | - 列表 | [链接](地址)
   ============================================ */

const POSTS = [
  {
    id: "riemann-zeta-continuation",
    title: "黎曼 ζ 函数的解析延拓:三跳迈向全平面",
    date: "2026-08-24",
    tags: ["数学", "复分析"],
    summary: "黎曼猜想谈论的零点位于级数不收敛的区域,所以得先把 ζ 解析延拓出去。η 交错级数推到 Re(s)>0,积分表示与 θ 模变换推到全平面并顺手得到函数方程,最后 sin 因子送出零点地图——纯公式推导,一步一跳。",
    content: `
黎曼猜想谈论的零点,全部位于 0 < Re(s) < 1 的**临界条带**;而 ζ 函数的定义级数只在 Re(s) > 1 收敛——级数在那里根本不定义 ζ。所以在谈零点之前,必须先把 ζ **解析延拓**出去。这篇文章把延拓过程拆成三跳,每一步只用到等比级数和最基本的积分。

## 出发点:只在半个平面有效的级数

\`\`\`text
ζ(s) = Σ 1/n^s = 1 + 1/2^s + 1/3^s + ...     (只在 Re(s) > 1 收敛)
\`\`\`

s 是复数,Re(s) 是实部。级数收敛与否只看实部:|n^(-s)| = n^(-Re(s)),虚部只贡献旋转、不改变大小。s = 1 处就是发散的调和级数。

延拓的合法性由**恒等定理**背书:两个解析函数哪怕只在很小的区域上相等,就在整个连通区域上相等。推论:延拓如果存在,必然唯一——延拓是"发现"函数的其余部分,不是"发明"。级数只是函数的一张局部照片,照片之外函数仍然存在,只是还没被算出来。

## 第一跳:η 函数——推到 Re(s) > 0

**加正负号,收敛域变宽。** 最直观的例子:

\`\`\`text
1 + 1/2 + 1/3 + 1/4 + ...   →  ∞
1 - 1/2 + 1/3 - 1/4 + ...   =  ln 2
\`\`\`

(ln 2 的来历:对 1/(1+x) = 1 - x + x² - ... 两边从 0 积到 1,左边积出 ln 1+x 在 0 到 1 上的积分值 = ln 2,右边逐项积分得到交错调和级数;积到端点的合法性由 Abel 定理保证。)

正负项互相抵消,驯服了发散:只要项的模单调趋于 0(即 Re(s) > 0),交错级数就收敛。定义 η(s) = Σ (-1)^(n-1) / n^s,与 ζ 逐项对照做减法:

\`\`\`text
ζ - η = 2·( 1/2^s + 1/4^s + 1/6^s + ... ) = 2^(1-s) · ζ
\`\`\`

偶数项提出 2^(-s) 后又变回 ζ 自己。于是 η(s) = (1 - 2^(1-s))·ζ(s),反解:

\`\`\`text
ζ(s) = η(s) / (1 - 2^(1-s))     (Re(s) > 0, s ≠ 1)
\`\`\`

在重叠区 Re(s) > 1 上两边逐点相等 → 恒等定理保证:它就是 ζ 在 Re(s) > 0 的**唯一真身**。第一跳完成:定义域推过了 s = 1 这条发散线。

## 第二跳:积分表示——通向全平面

η 只到 Re(s) > 0,左半平面还没有着落。关键想法:**级数没有"把手"**(要么整体收敛要么不收敛),**积分有**——坏点长在具体位置上,可以动手修理。

先写成积分。注意 1/(e^x - 1) 又是等比级数(公比 e^(-x)),逐项积分,每一项积出 Γ(s)/n^s:

\`\`\`text
1/(e^x - 1) = e^(-x) + e^(-2x) + e^(-3x) + ...
∫₀^∞ x^(s-1) · e^(-nx) dx = Γ(s) / n^s        (换元 u = nx)
⇒ Γ(s)·ζ(s) = ∫₀^∞ x^(s-1) / (e^x - 1) dx
\`\`\`

积分在 x → ∞ 处好得很(e^(-x) 指数衰减),**唯一的坏点在 x → 0**:那里 e^x ≈ 1 + x,所以 1/(e^x - 1) ≈ 1/x,被积函数 ≈ x^(s-2)——恰好需要 Re(s) > 1 才可积。和级数一模一样的门槛,但这次它长在一个可以动手修理的位置上。

**修法:把奇性显式减掉、单独积出。**

\`\`\`text
ζ(s) = 1/Γ(s) · ∫₀¹ x^(s-1)·( 1/(e^x - 1) - 1/x ) dx    ← 减掉 1/x,收敛区扩到 Re(s) > 0
     + 1/Γ(s) · ∫₁^∞ x^(s-1) / (e^x - 1) dx              ← 全平面收敛
     + 1/Γ(s) · 1/(s - 1)                                 ← ∫₀¹ x^(s-2) dx 的显式值
\`\`\`

减掉 1/x 赚到 Re(s) > 0;再减 -1/2、x/12、……每多减一项多赚一条带,把奇性减干净就覆盖全平面。这就是解析延拓的全部手艺:**把发散集中到一个局部位置,减掉,单独积出,剩下的收敛区域变大。**

**黎曼的做法(一次到位版)**:记 ψ(t) = Σ e^(-πn²t),利用 θ 函数的模变换 θ(t) = t^(-1/2)·θ(1/t),把 t → 0 端整个翻转到 t → ∞(那里指数衰减、天然收敛),一步得到:

\`\`\`text
Λ(s) := π^(-s/2) · Γ(s/2) · ζ(s)
      = ∫₁^∞ ψ(t) · ( t^(s/2 - 1) + t^((1-s)/2 - 1) ) dt + 1/( s·(s-1) )
\`\`\`

右边的积分对**一切复数 s** 都收敛。于是:ζ 延拓到全平面,唯一极点是 s = 1(残数 1)。

而且右边在 s ↔ 1-s 下不变,顺手**白送函数方程**:

\`\`\`text
Λ(s) = Λ(1-s)
⟺ ζ(s) = 2^s · π^(s-1) · sin(πs/2) · Γ(1-s) · ζ(1-s)
\`\`\`

两个著名特值:ζ(0) = -1/2,ζ(-1) = -1/12。级数在 s = -1 发散,但延拓后的函数在那里取有限值——这些值是解析性逼出来的唯一可能,不是人为约定。

## 第三跳:函数方程送出零点地图

看显式函数方程的四个因子:2^s · π^(s-1) 处处非零;Γ(1-s) 只有极点、不贡献零;真正会"造零"的只有 sin(πs/2)——它在负偶数处为零:

| 区域 | 零点情况 | 原因 |
|---|---|---|
| Re(s) > 1 | 无零点 | 欧拉乘积每个因子非零 |
| Re(s) < 0 | 恰好 -2, -4, -6, ...(平凡零点) | sin 因子在负偶数为零 |
| 0 ≤ Re(s) ≤ 1 | 其余全部零点(非平凡零点) | RH 的研究对象 |

**黎曼猜想**:所有非平凡零点都落在临界线 Re(s) = 1/2 上。1/2 这条线之所以特殊,正因为它是函数方程 s ↔ 1-s 这面镜子的**镜面所在**。

最后一步收尾:想定位这些零点,令 s = 1/2 + it,乘上一个模长恒为 1 的旋转因子,把复值的 ζ 转成**实值**的 Hardy Z 函数:

\`\`\`text
Z(t) = e^(iθ(t)) · ζ(1/2 + it)
θ(t) = Im ln Γ(1/4 + it/2) - (t/2)·ln π
|e^(iθ)| = 1  ⇒  Z(t) = 0  ⟺  ζ(1/2 + it) = 0
\`\`\`

于是"找零点"变成"找实值曲线穿越横轴的位置":

![Hardy Z 函数曲线](images/z-function.png)

曲线每次穿越横轴,就是一个非平凡零点 1/2 + t·i——它们密密地排向无穷,已验证的前 10^13 个全部精确落在 1/2 线上,但"全部"至今无人能证,这正是黎曼猜想。

## 写在最后

\`\`\`text
Σ 1/n^s  (Re > 1)
  → 第一跳: η 交错级数,推到 Re(s) > 0       [正负抵消]
  → 第二跳: θ 积分,推到全平面 + 函数方程     [翻转坏端]
  → 第三跳: sin 因子给平凡零点,临界条带装非平凡零点
\`\`\`

每跳的套路都一样:**找一个在更大区域收敛、且在重叠区与原表达式相等的新表达式**——恒等定理保证它是唯一真身。发散的从来只是表达式,不是函数。
`,
  },
  {
    id: "esp32-devboard",
    title: "ESP32-S3 开发板入门:从零画一块自己的开发板",
    date: "2026-08-23",
    tags: ["ESP32", "硬件设计", "PCB"],
    summary: "从原理图模块化开始,记录我用嘉立创EDA专业版画 ESP32-S3 开发板的完整过程:电源、USB转串口、自动下载电路,以及那些踩过的坑。",
    content: `
最近在画一块属于自己的 ESP32-S3 开发板。主控选了 **ESP32-S3-WROOM-1-N16R16V**(16MB Flash + 16MB PSRAM),外围电路其实都是成熟方案,但真正动手画一遍,理解完全不一样。

## 整体架构

一块最小系统的 ESP32 开发板,核心就五个模块:

- **电源模块**:USB 5V 输入,AMS1117-3.3 稳压到 3.3V
- **USB 转串口**:CH340C,负责和电脑通信
- **自动下载电路**:两只 NPN 三极管,经典 RTS/DTR 控制 EN/IO0
- **按键**:EN 复位键 + BOOT 下载键
- **扩展排针**:把 GPIO 全部引出来

## 原理图要模块化

第一版原理图被吐槽"太乱了"——元件随手放,有的甚至画到图框外面。正确的做法是**按功能分区**:

> 电源一个框、串口一个框、MCU 一个框,每个模块加上边框和标题,模块之间用网络标签(3V3、GND、EN、IO0)连接,而不是拉一堆长导线。

这样图纸的可读性会好非常多,后期查错也快。

## 双 USB 口的取舍

我一开始贪心画了两个 USB-C:一个 CH340C 串口口,一个 CH340N。后来才知道**两个 CH340 的 TX 同时接在 MCU 的 RX 上,两个口同时插电时会总线冲突**。常见的做法是:

1. 只留一个 CH340 做下载口
2. 第二个口接 ESP32-S3 的原生 USB(GPIO19/20),速度更快还支持 HID

## 坐标单位这个坑

嘉立创EDA 里原理图和 PCB 的坐标单位不一样:

| 域 | 单位 |
|---|---|
| PCB | 1 mil |
| 原理图 | 10 mil |

用错的话,元件会放到 10 倍远的地方去。

下一篇文章写 PCB 布局的思路,敬请期待。
`,
  },
  {
    id: "python-notes-kwargs",
    title: "Python 学习笔记:从一个 headers 字典到 **kwargs",
    date: "2026-08-22",
    tags: ["Python", "学习笔记"],
    summary: "从调用 API 时的 headers 字典出发,一路搞懂字典嵌套列表、链式取值、元组的逗号陷阱,最后打通 **kwargs 和解包赋值。",
    content: `
这周在读 API 示例代码时被一行代码卡住了:

\`\`\`python
prediction_id = generate_result["data"]["id"]
\`\`\`

顺着这行代码,把 Python 的几个基础概念彻底搞明白了,记个笔记。

## 字典的值可以是任何东西

\`\`\`python
data = {
    "model": "seedance-2.5",
    "reference_images": [
        "https://example.com/1.jpg",
        "https://example.com/2.jpg",
    ],
}
\`\`\`

字典套列表、列表套字典,层层嵌套——这不是语法混乱,这就是 **JSON 的 Python 写法**。\`json.dumps(data)\` 一转,就能直接当请求体发出去。

## 链式取值从左往右剥

\`result["data"]["id"]\` 的求值过程:

1. \`result["data"]\` 先取出内层字典
2. \`["id"]\` 再从内层字典里取值

不管嵌套多少层都是这个规则。稳妥写法用 \`.get()\`:

\`\`\`python
prediction_id = generate_result.get("data", {}).get("id")
\`\`\`

取不到得到 \`None\` 而不是直接崩,方便先判断再处理。

## 元组是由逗号定义的

最容易踩的坑:

\`\`\`python
a = ("x", "y")   # 元组,两个元素
b = ("x" "y")    # 字符串自动拼接,得到 "xy"
c = ("xy",)      # 单元素元组,逗号不能省
\`\`\`

**判断标准:看逗号,不看括号。**

## **opts 不是指针

C 语言的 \`**\` 是二级指针,Python 里完全是另一回事:

- \`def f(**opts)\` —— 把多余的关键字参数**打包**成字典
- \`f(**d)\` —— 把字典**摊开**成关键字参数
- \`2 ** 10\` —— 乘方

一收一放,互为逆操作。读 pymavlink 源码时看到它把连接字符串里的 \`baud=57600\` 解析出来塞进 opts 字典,和关键字参数合并处理,设计得挺优雅。

## if __name__ == "__main__"

\`__name__\` 是 Python 自动填的变量:直接运行时等于 \`"__main__"\`,被 import 时等于模块名。所以这段代码的意思是:**只有直接运行本文件才执行,被导入时不执行**。让一个文件既能当脚本跑、又能当模块被复用。

接下来准备拿这些知识写一个调用视频生成 API 的完整脚本:提交任务 → 轮询状态 → 下载视频。
`,
  },
  {
    id: "ai-video-overview",
    title: "AI 视频生成初探:Seedance 和它的对手们",
    date: "2026-08-20",
    tags: ["AI", "视频生成", "Seedance"],
    summary: "想用 Seedance 的参考图生视频功能把自己旅行照片变成奇幻短片,顺手把 2026 年的 AI 视频格局和技术原理整理了一遍。",
    content: `
最近想搞个好玩的事情:用 AI 视频模型的"参考图生视频"功能,把博物馆旅行照片变成一段"壁画苏醒"的奇幻短片。顺便把现在的 AI 视频技术梳理了一遍。

## 底层原理:扩散模型 + Transformer

现在主流视频模型的技术骨架高度趋同,大致三步:

1. **压缩**:用 3D VAE 把视频在空间和时间两个维度压成低维潜空间,运算量降几个数量级
2. **去噪生成**:从纯噪声出发,Transformer 以提示词(和参考图)为条件一步步去噪
3. **解码**:潜空间还原回像素,输出视频帧

图生视频(I2V)是实用性最强的模式——给一张图,模型让它动起来,可控性远高于纯文生视频。

## 2026 年的格局

- **Seedance 2.0**(字节):图生视频最强,即梦 AI 背后的引擎
- **Sora 2**(OpenAI):画面细节和真实感领先
- **Veo 3.1**(Google):音画同步、物理真实感最好
- **Kling 3.0**(快手):长视频、社交短片见长

## 提示词的写法

实测下来,效果稳定的提示词都遵循这个结构:

> 主体+服装 → 动作 → 环境变化 → 镜头语言 → 光影 → 整体风格

比如我的"壁画苏醒"提示词:

\`\`\`text
幽暗的展馆长廊里,黑色长直发的年轻女孩缓步走向一幅巨大的
民族风格壁画……她停下脚步抬头凝视,画中人物的眼睛忽然缓缓
亮起,飞天的飘带破壁而出……镜头从女孩背后缓慢推近至侧脸,
再环绕上升。电影质感,奇幻光影,色彩浓烈饱和。
\`\`\`

要点:**一次一镜**,别贪心写多镜头剪辑;时长先生成 5 秒验证,满意再拼。

## 一些实用关键词

想让画面更梦幻,可以在结尾加这些词:\`轻微的慢动作\`、\`浅景深\`、\`体积光\`。

等视频生成好了,下一篇写个 Python 脚本把"提交任务 → 轮询 → 下载"全流程自动化。
`,
  },
];
